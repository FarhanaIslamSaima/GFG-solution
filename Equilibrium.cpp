#include <iostream>
#include <map>
#include<string.h>
#include <limits.h>
#include <vector>
#include <bits/stdc++.h>
#include<array> 
#include <unordered_map>
#include<cmath>

using namespace std;
int main(){
    int a,b;
    cin>>a;
    int r1,r2,l1,l2;
    int countL=0;
    int countR=0;
 
    vector<int>num;
    for(int i=0;i<a;i++){
        cin>>b;
        num.push_back(b);

    }
    for(int i=0;i<a;i++){
       l1=i,l2=i+1,r1=n-1,r2=n-2;
       while(l2<r2){
        countL=l1+l2;
        countR=r1+r2;
        if(countL==countR){
            
        }

       }
    }





    return 0;
}